{
    "name": "@ios-shortcuts/scriptable-simulator",
    "version": "1.0.0",
    "main": "./dist/index",
    "types": "./dist/index",
    "exports": {
        ".": "./dist/index.js"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc --project tsconfig.build.json",
        "clean": "rimraf ./dist",
        "prepublishOnly": "pnpm run build"
    },
    "devDependencies": {
        "@types/node": "^20.4.9",
        "rimraf": "~5.0.0",
        "typescript": "~4.9.0"
    },
    "dependencies": {
        "@ios-shortcuts/scriptable-environment": "workspace:*"
    }
}
