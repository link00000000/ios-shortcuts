/**
 * Imports module with specified name.
 */
export declare class importModule {

    /**
     * Modules are imported by specifying the name of the file. For example, to import the
     * file foo.js, call importModule('foo'). Including the file extension is optional.Scriptable will look for modules in the following directories, in order:     */
    public (r) => {
      return Math.PI * Math.pow: void): void;
}
