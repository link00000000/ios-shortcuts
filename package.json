{
    "name": "ios-shortcuts",
    "private": true,
    "scripts": {
        "actions": "act",
        "build": "pnpm build:shortcuts",
        "build:packages": "pnpm --filter ./packages/* build",
        "build:shortcuts": "pnpm --filter ./shortcuts/* build",
        "build:tools": "pnpm --filter ./tools/* build",
        "preinstall": "npx only-allow pnpm",
        "repl": "node -r ./packages/scriptable/dist/simulator/config -i",
        "simulate": "node -r ./packages/scriptable/dist/simulator/config"
    },
    "devDependencies": {
        "@dagger.io/dagger": "^0.8.4",
        "typescript": "~4.9.0"
    }
}
